import HighlightPublicationStatusDirective from './highlight-publication-status.directive';

describe('HighlightPublicationStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightPublicationStatusDirective();
    expect(directive).toBeTruthy();
  });
});
